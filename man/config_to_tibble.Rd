% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_to_tibble.R, R/deprecations.R
\name{config_to_tibble}
\alias{config_to_tibble}
\alias{config2tibble}
\title{Convert GeoPressureTemplate config.yml to tibble}
\usage{
config_to_tibble(
  file = Sys.getenv("R_CONFIG_FILE", "config.yml"),
  filter_return = TRUE
)

config2tibble(
  file = Sys.getenv("R_CONFIG_FILE", "config.yml"),
  filter_return = TRUE
)
}
\arguments{
\item{file}{Character string specifying the path to the \code{config.yml} file.}

\item{filter_return}{Logical. If \code{TRUE}, only columns that vary across tags are returned.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per tag ID and columns for each configuration
parameter. The exact columns returned depend on the configuration file structure and
the \code{filter_return} parameter.
}
\description{
Reads a GeoPressureTemplate \code{config.yml} file and converts it into a tibble format with
one row per tag and columns for each configuration parameter. This is useful for comparing
configurations across multiple tags or for programmatic analysis of tag settings.

\code{config2tibble()} was renamed to \code{\link[=config_to_tibble]{config_to_tibble()}}.
}
\details{
This function uses \code{\link[GeoPressureR:geopressuretemplate]{GeoPressureR::geopressuretemplate_config()}} internally to parse
each tag's configuration. It then flattens the nested list structure and applies
appropriate type conversions for each parameter (e.g., converting dates to strings,
functions to their deparsed representation).

The function handles various GeoPressureR configuration parameters including:
\itemize{
\item Tag creation settings (manufacturer, file paths, crop dates)
\item Labeling parameters (flight/stap duration warnings)
\item Map settings (extent, known locations, pressure/light thresholds)
\item Graph creation and movement model parameters
\item Bird characteristics (mass, wing measurements)
\item Wind and environmental data settings
}
}
\examples{
\dontrun{
# Read config from default location
cfg <- config_to_tibble()

# Read from specific file
cfg <- config_to_tibble("path/to/config.yml")

# Include all columns even if they don't vary
cfg_full <- config_to_tibble(filter_return = FALSE)

# View which parameters differ across tags
View(cfg)
}

}
\seealso{
\itemize{
\item \code{\link[GeoPressureR:geopressuretemplate]{GeoPressureR::geopressuretemplate_config()}} for the underlying config parser
\item \href{https://github.com/Rafnuss/GeoPressureTemplate}{GeoPressureTemplate} for the
template structure
}
}
